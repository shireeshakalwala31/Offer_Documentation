<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Relieving and Experience Letter</title>
  <style>
    @page {
      size: A4;
      margin: 0;
    }

    body {
      font-family: 'Calibri', Arial, sans-serif;
      font-size: 12px;
      line-height: 1.75;
      color: #000;
      margin: 0;
      padding: 0;
      background: url("/assets/letterhead.jpeg") no-repeat center top;
      background-size: cover;
    }

    .content {
      position: relative;
      width: 75%;
      margin: 0 auto;
      top: 165px;
      font-size: 13px;
      line-height: 1.8;
    }

    .date {
      text-align: left;
      margin-bottom: 16px;
    }

    .title {
      text-align: center;
      font-weight: bold;
      text-decoration: underline;
      font-size: 16px;
      margin-top: 0;
      margin-bottom: 50px;
    }

    /* ✅ Increased gap between heading and Employee Name */
    .gap-after-title {
      height: 35px; /* increased from 25px → 55px */
    }

    .paragraph {
      margin: 0 0 9pt 0;
      text-align: justify;
    }

    /* ✅ Add spacing after Employee details section */
    .employee-section {
      margin-bottom: 20px;
    }

    .closing {
      margin-top: 20pt;
      line-height: 1.3;
       text-align: left;
      
    }

    .signature-block {
      margin-top: -9pt;
      text-align: left;
      line-height: 1.1;
     margin-left: 5mm;
  margin-bottom: 0;
    }

    .signature-block img {
      width: 85px;
      height: auto;
      opacity: 0.95;
      margin-top: -2pt;
       
    }

    .hr-details {
      margin-top: -9pt;
      margin-bottom: 25px ;
      line-height: 1.2;
    }

    .note {
      font-size: 10pt;
      margin-top: 10pt;
      margin-bottom: 30px;
      position: relative;
      text-align: left;
    }
  </style>
</head>
<body>
  <div class="content">
    <p class="date">
      <%= new Date(relieving_date).toLocaleDateString('en-GB').replace(/\//g, '-') %>
    </p>

    <p class="title">Relieving and Experience Letter</p>

    <!-- ✅ Larger gap after heading -->
    <div class="gap-after-title"></div>

    <!-- ✅ Employee details block -->
    <div class="employee-section">
      <p class="paragraph"><strong>Employee Name:</strong> <%= employee_name %></p>
      <% if (employee_id) { %>
        <p class="paragraph"><strong>Employee ID:</strong> <%= employee_id %></p>
      <% } %>
    </div>

    <p class="paragraph">This is with regard to your resignation,</p>

    <p class="paragraph">
      Dated
      <%= new Date(start_date).toLocaleDateString('en-GB').replace(/\//g, '-') %>
      from the position of <strong><%= designation %></strong> — We would like to inform you that you have been relieved of your duties at the end of business hours of
      <%= new Date(resignation_date).toLocaleDateString('en-GB').replace(/\//g, '-') %>.
    </p>

    <p class="paragraph">
      We enjoyed working with you and thank you for the service rendered from
      <strong><%= new Date(start_date).toLocaleDateString('en-GB').replace(/\//g, '-') %></strong>
      to
      <strong><%= new Date(resignation_date).toLocaleDateString('en-GB').replace(/\//g, '-') %></strong>.
    </p>

    <p class="paragraph">We wish you good luck for future endeavours.</p>

    <div class="closing">
      <p>Yours sincerely,</p>
      <p>For Amazon IT Solutions</p>
    </div>

    <% if (signaturePath) { %>
      <div class="signature-block">
        <img src="<%= signaturePath %>" alt="Signature" />
      </div>
    <% } %>

    <div class="hr-details">
      <p><%= hrName %></p>
      <p><strong><%= hrDesignation %></strong></p>
    </div>

    <p class="note">
      Note: This is a computer-generated document. No Signature is required*
    </p>
  </div>
</body>
</html>
